根据图片内容，提取的文字如下：

CBS?

<1 在已经计算完历史金额后，写入到临时缓存文件中，已计算完的可设置一天有效期，有效期内可不重复劳动。最后各批次均计算完后合并整理再统一计算变量。

<2 计算过程中，控制进程数，不要将服务器CPU吃满。

<3 设置水位，占CPU 80%水位，动态监测，如Top指令。

<4 数据抽调与自动化修补单元，独立模块化，每天或每几小时，执行一次调度，进行实时监控与数据修补。在出帐前，准备好数据，避免浪费业务运行时间。

<5 数据平滑处理，如果在某一个周期里，某一天无数据，直接分析是否是假日、周末，如果是交易日，前后均有数据，但接口无数据，则平滑将前一天复写在该空白日，或直接写0。

<5 前位分析，加一个上市时间判断，参数要求2023年至今数据，但24