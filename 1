在复盘模块中新增回测单元，该单元通过读取外部配置文件（Configure File）中的历史交易记录，自动拉取对应标的的历史与实时行情数据，计算历史入仓盈亏及目标价达成情况，最终将结果格式化输出至Excel文件的新Sheet页中，辅助交易员进行复盘分析。



功能需求 (Functional Requirements)


1 配置文件读取模块

输入源：支持读取指定路径下的配置文件（如  pool.yaml ）。

解析字段：系统需准确解析以下三个核心字段：

 stock_code ：入仓股票代码（如：600519）

 entry_time ：入仓时间（精确到日，格式：YYYYMMDD）

 entry_shares ：入仓股数（整数）

异常处理：若配置文件缺失字段、格式错误或股票代码无效，系统需抛出明确的错误日志并跳过该股票。


2.2 数据获取与计算引擎

历史价格获取：根据  entry_time  获取该标的当天的收盘价（若遇非交易日，需自动顺延前一个交易日）。

实时价格获取：获取该标的当前最新时间的收盘价。

盈亏率计算：

公式： 盈亏率 = (当前收盘价 - 入仓收盘价) / 入仓收盘价 * 100% 

目标价计算

获取当前时间系统生成的  T1_Target  与  T2_Target 。


2.3 Excel 导出模块

文件操作：Excel报告中中新建一个独立的Sheet页（命名规则建议： 跟仓回测 ）。

表头规范：

股票代码

股票名称

入仓时间

入仓收盘价格

入仓股数

当前收盘价格

盈亏率

当前T1目标价

当前T2目标价

格式要求：价格保留两位小数，盈亏率保留两位小数并带  %  后缀，股数为整数。

数据从表中提取，