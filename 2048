2048「特技加速」模式 —— 完整产品需求文档（PRD）

1. 功能概述

在经典2048玩法基础上，新增“特技加速”变现模块。玩家每合成一个1024方块，即获得一次“特技机会”。该机会会短暂闪烁提示，随后存入底部“特技池”。玩家可随时点击池中特技，观看激励视频广告后，直接获得额外分数奖励（1024×2=2048分），且完全不干扰当前棋盘布局，实现“无损加速冲榜”。

2. 触发规则（核心机制）

· 唯一触发条件：每次移动合并后，新生成的方块数字恰好等于 1024（即 512+512 合并）。合成2048、4096等更高等级均不触发。
· 单局上限：每局游戏最多累积 10次 特技机会（达到上限后，后续合成1024将不再触发任何提示，完全静默）。
· 存储方式：触发的特技按先进先出（FIFO） 顺序存入底部特技池队列。

3. 棋盘交互反馈（“10秒闪击”）

· 出现位置：合成1024的瞬间，在棋盘正中央悬浮出现特技按钮。
· 视觉样式：半透明磨砂圆形，内置 ⚡ 闪电图标 + 标注 “×1024”，带有脉冲呼吸光效。
· 倒计时：按钮周围显示环形进度条，10秒匀速缩短。
· 覆盖逻辑（重要）：若10秒倒计时未结束，玩家又合成了新的1024：
  · 当前倒计时立即终止，当前按钮沉入底部队列。
  · 新按钮立刻在棋盘中央刷新，重新开始10秒倒计时。
· 倒计时结束：按钮渐隐消失，该次特技自动存入底部特技池。

4. 底部“特技池”UI与操作（核心变现入口）

4.1 主入口状态

· 位于棋盘正下方（分数与操作按钮之间），固定显示：
  · ⚡ 特技池 [ 当前库存数 / 10 ]
  · 有库存（>0）：图标高亮且带呼吸动画。
  · 无库存（=0）：图标置灰。

4.2 弹窗列表（点击主入口后弹出）

· 弹窗为半透明浮动列表，按触发时间顺序从上到下展示所有待使用的特技条目（每个条目统一显示为 1024，可附加序号，如 #1 1024）。
· 自由选择策略：玩家可点击列表中任意一条特技，优先消耗哪条由玩家决定（全部奖励相同，此设计仅为后续扩展预留，当前无需区分等级）。

4.3 广告与奖励闭环（无任何“白嫖”路径）

· 玩家点击任意一条特技 → 立即加载并播放激励视频广告（无二次确认弹窗）。
· 广告看完（回调成功）：
  · 总分 直接增加 2048 分（计算逻辑：1024 × 2）。
  · 该条目从列表中移除，库存数减1。
  · 屏幕中央飘字特效：🚀 +2048 特技加速！
· 广告失败/中途关闭/未加载完成：
  · 条目恢复为高亮可点状态，奖励不予发放，库存数不变。
  · 玩家可随时再次点击该条目重试。

5. 上限已满后的静默处理

当特技池库存 = 10 时：

· 后续再合成1024，棋盘中央不再弹出任何闪烁按钮。
· 该次合成不存入底部池子。
· 无任何弹窗或Toast提示（保持游戏流畅，避免骚扰）。

6. 开发边界与容错清单（防坑必看）

场景 处理方案
快速连续触发 旧闪烁立即沉底，新闪烁顶替，保障队列只丢失视觉提示，但数据不丢失（旧机会已入队）。
弹窗内快速连点 点击条目后立即置灰 + 1秒点击冷却，防止重复请求广告SDK引发报错。
点击特技时棋盘滑动冲突 点击闪烁按钮或弹窗条目时，拦截触控事件，阻止下层棋盘响应滑动。
分数数据类型 加速奖励累加可能使总分突破数百万，必须使用 Long（64位整型） 存储分数。
游戏中途退出/重启 特技池队列数据需持久化存储（存入本地SharedPreferences或文件），确保玩家重进游戏时库存不丢失。
广告加载超时 设置加载超时（如5秒），超时后提示“广告加载中，请稍后重试”，不扣减特技次数。

7. 数据埋点建议（运营分析用）

· 触发率：每局合成1024的次数 vs 实际触发特技的次数（判断是否受10次上限影响）。
· 点击率：棋盘闪烁按钮的点击率（衡量“10秒紧迫感”是否有效）。
· 消耗率：底部池子特技被消耗的比例 vs 对局结束时仍积压的比例（判断玩家是否习惯“攒到最后冲刺”）。
· 广告填充率：各广告位在不同时段的填充成功率。